<UserControl x:Class="PropertyManagement.Client.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="微软雅黑">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding Message}" Margin="0,12,0,0" FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center" TextWrapping="Wrap" />
            <Border Background="#E8F1FD" CornerRadius="6" Margin="0,24,0,0" Padding="18,10">
                <TextBlock Text="该模块将在 M4~M6 按业务切片实现（M3 仅交付全局框架三页）"
                           Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
