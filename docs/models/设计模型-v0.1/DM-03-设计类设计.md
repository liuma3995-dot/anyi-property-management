# DM-3 设计类设计

> 制品：DM-03 ｜ 版本：v0.2（前后端分离）｜ 状态：待评审
> 输入：AM-03 概念（43 个）、AM-02 用例、DM-01/02 架构与组件、技术选型建议
> 说明：实体-服务-仓储-视图模型四类职责分离；实体/服务/仓储位于后端，视图模型/API 客户端位于前端。

## 一、包结构

```mermaid
flowchart TB
  subgraph 前端
    VM["Views / ViewModels / ApiClient"]
  end
  subgraph 后端
    API["Api（控制器）"]
    SVC["application.*（应用服务）"]
    DOM["domain.*（领域层：实体/服务/仓储接口）"]
    INF["infrastructure.*（仓储实现/文件/日志）"]
  end
  VM -->|"HTTP DTO"| API
  API --> SVC
  SVC --> DOM
  DOM --> INF
```

## 二、分层类职责

| 层 | 类类型 | 职责 | 示例（财务） |
|---|---|---|---|
| 前端表现层 | 视图模型（ViewModel）/页面/API 客户端 | 界面状态、命令绑定、HTTP 调用 | PaymentViewModel、BillingApiClient |
| 后端 API 层 | 控制器（Controller） | 路由、鉴权、DTO 转换 | PaymentsController |
| 后端应用服务层 | 用例服务 | 用例编排、事务边界、调用领域规则 | PaymentService |
| 后端领域层 | 实体（Entity）/领域服务/仓储接口 | 状态与不变量、跨实体规则、持久化抽象 | Bill、Payment、IBillRepository |
| 后端基础设施层 | 仓储实现 | 数据库读写 | SqlBillRepository |

## 三、核心领域类图（4 张）

### 3.1 财务领域

```mermaid
classDiagram
  class ChargeItem {
    +名称
    +单价
    +计费模式(年/月/临时)
  }
  class BillingCycle {
    +周期类型
    +起止日期
  }
  class Bill {
    +金额
    +状态(待缴/部分缴/逾期/已缴/已冲正)
    +到期日
  }
  class Payment {
    +金额
    +方式
    +时间
  }
  class Receipt {
    +收据号
    +打印次数
  }
  class RefundAdjustment {
    +类型
    +金额
    +原因
  }
  class PreDeposit {
    +余额
  }
  class Expense {
    +金额
    +日期
  }
  ChargeItem --> Bill
  BillingCycle --> Bill
  Bill --> Payment
  Payment --> Receipt
  Payment --> PreDeposit : 多缴转存
  Bill --> RefundAdjustment
  Expense --> ChargeItem
```

### 3.2 基础信息领域

```mermaid
classDiagram
  class Community
  class Building
  class Unit
  class Property {
    +房号
    +面积
    +用途
    +入住状态
  }
  class Owner {
    +姓名
    +联系方式
  }
  class OwnerPropertyRelation {
    +关系类型(自住/出租)
    +生效时间
  }
  class ParkingSpace {
    +车位号
    +类型(固定/临时)
  }
  Community "1" --> "*" Building
  Building "1" --> "*" Unit
  Unit "1" --> "*" Property
  Property "1" --> "*" OwnerPropertyRelation
  Owner "1" --> "*" OwnerPropertyRelation
  ParkingSpace --> Property : 绑定
```

### 3.3 应急领域

```mermaid
classDiagram
  class EmergencyScene {
    +名称
    +类别
  }
  class EmergencyStep {
    +序号
    +内容
    +版本号
  }
  class EmergencyEvent {
    +时间
    +地点
    +状态(已发起/处置中/已结案/已复盘)
  }
  class EmergencyAssign {
    +责任人
    +值班人
  }
  class EmergencyRecord {
    +时间
    +操作
    +结果
  }
  class ReviewRecord {
    +原因
    +改进措施
  }
  EmergencyScene "1" --> "*" EmergencyStep
  EmergencyScene --> EmergencyEvent
  EmergencyEvent "1" --> "*" EmergencyAssign
  EmergencyEvent "1" --> "*" EmergencyRecord
  EmergencyEvent "1" --> "0..1" ReviewRecord
```

### 3.4 设备领域

```mermaid
classDiagram
  class DeviceType {
    +名称
    +保养周期
  }
  class Device {
    +名称
    +位置
    +状态(在用/维修中/停用/报废)
  }
  class MaintenanceRecord
  class InspectionRecord
  class FaultRecord {
    +现象
    +原因
    +处理
  }
  class Vendor {
    +单位名称
    +联系人
  }
  DeviceType --> Device
  Device --> MaintenanceRecord
  Device --> InspectionRecord
  Device --> FaultRecord
  Vendor --> MaintenanceRecord
  Vendor --> InspectionRecord
```

## 四、设计原则

1. 实体承载状态与规则（如 Bill 的"部分缴/逾期"判定），服务编排用例（PaymentService 组织校验、记账、出票、审计）；
2. 依赖倒置：领域层定义仓储接口，基础设施层实现，领域层不引用任何框架；
3. 视图模型与实体分离，界面不直接操作实体；
4. 每个 AM-03 概念至少映射一个实体类（43 个概念全覆盖），其余为服务/仓储/视图模型类；
5. 技术选型确认后（D-1/D-2），按框架映射本设计（类图不变，只增加注解/映射）。
