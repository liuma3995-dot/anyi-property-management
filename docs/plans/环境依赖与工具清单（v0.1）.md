# 环境依赖与工具清单（v0.1）

> 版本：v0.1 ｜ 日期：2026-08-28 ｜ 状态：待评审
> 依据：M0 环境核查（2026-08-28）＋ [项目开发计划（v0.1）](项目开发计划（v0.1）.md)（DZ-3 数据目录、DZ-4 验收策略）
> 说明：本清单为"当前机器"（负责人开发机）的环境盘点；按里程碑补装，不阻塞 M1/M2。

## 一、已就绪（无需处理）

| 项 | 状态 |
|---|---|
| .NET Framework 4.8 运行时 | ✅ 已装（Release 533325） |
| 构建链：MSBuild 4.8 / csc 4.8 / NuGet.exe 7.9 | ✅ 已就绪（引用程序集由 NuGet 包提供，无需 Developer Pack） |
| Visual Studio 2026 Community（.NET 桌面开发工作负载） | ✅ 已装（F:\.NET，2026-08-28） |
| .NET Framework 4.8 目标包 + SDK + Developer Pack 引用程序集 | ✅ 已装（随 VS 工作负载，`C:\Program Files (x86)\Reference Assemblies\...\v4.8` 已存在） |
| VS MSBuild（F:\.NET\MSBuild\Current\Bin\MSBuild.exe） | ✅ 已用其编译 PropertyManagement.sln 通过（三工程） |
| Git 2.53 | ✅ |
| VS Code（D 盘）+ 中文语言包 + pencildev 扩展 | ✅ |
| winget 1.9（提权可用） | ✅ 可用来补装工具 |
| curl.exe / PowerShell / 网络可达 nuget.org（提权） | ✅ |
| 硬件：i7-7700K / 16GB 内存 / D 盘 107GB、G 盘 102GB 可用 | ✅ |
| 验收机：负责人 Win10 真机（本机） | ✅ 按 DZ-4 作为唯一验收环境 |
| Office（查看导出的 Excel） | ✅ |
| 虚拟打印机：Microsoft Print to PDF（默认） | ✅ 可先行验证收据/报表输出 |

## 二、待补清单

### P0 开发必需（M3 前端开发前必须补齐）

> ✅ **P0 已完成（2026-08-28）**：Visual Studio 2026 Community 已装于 `F:\.NET`（.NET 桌面开发工作负载 + .NET Framework 4.8 目标包/SDK），VS MSBuild 编译验证通过。后续 M3 前端开发无需再补任何 P0 工具。

### P1 里程碑依赖（到点再装，不阻塞当前）

| # | 工具/依赖 | 用途 | 需要时点 | 下载地址 | 安装方式 |
|---|---|---|---|---|---|
| 3 | NuGet 包：System.Data.SQLite + Dapper + BCrypt.Net-Next | 数据访问与密码哈希 | M2 | （无需手动下载） | `nuget restore` 自动还原 |
| 4 | NuGet 包：CommunityToolkit.Mvvm | MVVM 基建 | M3 | （无需手动下载） | restore；引入时验证 MSB3258 兼容性 |
| 5 | NuGet 包：ClosedXML + PDFsharp | Excel/PDF 报表 | M4 | （无需手动下载） | restore |
| 6 | DB Browser for SQLite（可选） | 数据库调试 | M2 | https://sqlitebrowser.org/dl/ | 下载 Windows 安装包；或 `winget install sqlitebrowser.sqlitebrowser` |
| 7 | Inno Setup 6 | 安装包制作 | M8 | https://jrsoftware.org/isdl.php | 下载最新版安装包；或 `winget install JRSoftware.InnoSetup` |
| 8 | .NET Framework 4.8 离线安装器（ndp48-x86-x64-allos-enu.exe，约 120MB） | Win7 部署时内嵌安装包 | M8 | https://go.microsoft.com/fwlink/?linkid=2088631 | 下载离线包并保存到发布资源目录（供 Inno Setup 内嵌） |
| 9 | 物理打印机（可选） | 真实收据打印验收 | 部署验收 | （硬件，无下载） | 开发/验收先用默认"Microsoft Print to PDF"虚拟打印机 |

### P2 可选

| # | 工具 | 用途 | 下载地址 | 安装方式 |
|---|---|---|---|---|
| 10 | 7-Zip | 解压/打包辅助 | https://www.7-zip.org/download.html | 下载 64 位 x64 安装包；或 `winget install 7zip.7zip` |
| 11 | Postman | API 自测（已有 curl 可替代） | https://www.postman.com/downloads/ | 下载 Windows 安装包；可选 |

### 已明确不需要

- Win7 虚拟机 / 虚拟机软件（DZ-4：不使用虚拟机兜底）；
- Docker（无法替代 Win7 桌面验证，本项目无容器化需求）；
- 独立 git 服务器/远程仓库（本地单机开发，main 分支即可）。

## 三、补装建议

1. **当前**：VS 已就绪（F:\.NET），M1/M2/M3 均可直接开工；
2. **M2 期间**：视需要装 DB Browser for SQLite（调试数据库）；
3. **M4 收据/报表验证**：用默认虚拟打印机先行；物理打印机待真机部署时由负责人提供；
4. **M8 前**：装 Inno Setup、下载 .NET 4.8 离线安装器。

## 四、评审确认

1. 本清单是否与实际环境一致？
2. VS 已装（2026-08-28）✅，P0 全部完成；
3. 是否委托我用 winget 补装 P2 项（7-Zip 等）？
