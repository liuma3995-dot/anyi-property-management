-- ============================================================
-- migration_041.sql（v1.1.0 第 16 轮：收费项目缴费对象口径）
-- 1) 存量「按卡」(card) 收费项目的缴费对象统一为「业主」：
--    按卡项目本质按人（持卡人=业主）计费，历史数据由计价方式派生为「房产」属口径偏差。
-- 2) 其余计价方式（按户/一次性/分摊等）保持原值，由维护人员在收费项目表单中按需调整。
-- 3) object_type：0 房产 / 1 车位 / 2 业主（ChargeObjectType，CHG-v1.1.0-16 新增 2）
-- 版本：041（2026-09-17）
-- ============================================================

UPDATE t_charge_item
   SET object_type = 2,
       updated_at  = datetime('now','localtime')
 WHERE del_flag = 0
   AND method_code = 'card'
   AND object_type = 0;
