-- ============================================================
-- migration_051.sql（v1.1.2：计量变量补「年数」）
-- 背景：负责人反馈「按年计费的收费标准只按 1 个月计费」——
--       周期派生变量原按账期日期跨度折算，账期不足一月时月数恒为 1；
--       现口径改为「规格计费周期优先」（每年 → 月数 12），并补一个「年数」变量，
--       便于按年计价的收费标准直接写「单价 × 面积 × 年数」或按「月数」折算。
-- 来源：1 周期派生（与月数 / 天数同口径，由计费周期自动折算，不可手填）。
-- 说明：幂等插入（INSERT OR IGNORE），可重复执行；内置变量可停用、不可删除。
-- 版本：051（2026-09-20）
-- ============================================================

INSERT OR IGNORE INTO t_charge_variable
    (var_code, var_name, unit, value_type, source, default_value, object_scope, field_key, is_builtin, remark, status, del_flag, sort)
VALUES
    ('MQ-19', '年数', '年', 1, 1, NULL, 4, NULL, 1,
     '由计费周期自动折算（每年 1、每半年 0.5、每季 0.25、每月 0.0833）；自定义周期按账期跨度折算', 0, 0, 19);
