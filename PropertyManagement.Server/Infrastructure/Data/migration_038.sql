-- ============================================================
-- migration_038.sql（v1.1.0 第 9 轮：生成账单「缴费对象范围」留痕）
-- 1) t_bill_generate_log 新增 scope_summary：记录本次出账范围
--    （如「指定 12 个缴费对象（房产 12）」），用于事后对账与失败重推核对。
-- 2) 存量批次 scope_summary 保持 NULL（历史批次无「范围」概念，不臆造值）。
-- 版本：038（2026-09-17）
-- ============================================================

ALTER TABLE t_bill_generate_log ADD COLUMN scope_summary TEXT;
