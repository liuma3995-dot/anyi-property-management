-- ============================================================
-- migration_027：到期提醒责任班组可自定义（R7）
-- 1) t_reminder 新增 responsible_team：责任班组人工指派值（空=按默认规则：逾期→物业办，其余→工程部）
-- 版本：027（2026-09-11）
-- 说明：仅新增列；历史行保持为空，展示口径与既有规则一致。
-- ============================================================

ALTER TABLE t_reminder ADD COLUMN responsible_team TEXT;
