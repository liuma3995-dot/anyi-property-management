-- ============================================================
-- migration_056.sql（v1.1.2：参数字典「车位类型」文案 人防 → 普通）
-- 背景（负责人 2026-09-20 指令）：新增车位表单的「类型」需把「人防」改为「普通」；
--       参数字典维护里同一维度的字典项仍写「人防车位」，不改会与界面口径不一致。
-- 处置（CHG-v1.1.2-48）：把 type_code='parking_type' 且 item_name 含「人防」的字典项文案改为「普通车位」；
--       字典项代码（civil）与车位类型值（1）保持不变 —— 历史数据与 BR-INF-03（该类型禁售）约束不受影响。
-- 幂等：仅当 item_name 仍含「人防」时更新，可重复执行。
-- 版本：056（2026-09-20）
-- ============================================================

UPDATE t_dict_item
SET item_name = '普通车位'
WHERE type_code = 'parking_type'
  AND item_code = 'civil'
  AND item_name LIKE '%人防%';
